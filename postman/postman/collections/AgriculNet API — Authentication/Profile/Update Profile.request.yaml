$kind: http-request
method: PATCH
url: '{{base_url}}/api/{{api_version}}/auth/me'
order: 2000
headers:
  - key: Content-Type
    value: application/json
  - key: Authorization
    value: 'Bearer {{access_token}}'
body:
  type: json
  content: |-
    {
      "city": "Buea"
    }
