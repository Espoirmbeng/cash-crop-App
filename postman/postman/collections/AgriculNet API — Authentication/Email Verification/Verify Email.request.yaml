$kind: http-request
method: POST
url: '{{base_url}}/api/{{api_version}}/auth/verify-email'
order: 1000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "token": "sample-token"
    }
