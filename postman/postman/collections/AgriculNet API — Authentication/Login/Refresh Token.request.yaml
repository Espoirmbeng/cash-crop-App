$kind: http-request
method: POST
url: '{{base_url}}/api/{{api_version}}/auth/refresh-token'
order: 3000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "refreshToken": "{{refresh_token}}"
    }
