export function CropBadge({ children }) {
  return (
    <span className="inline-flex items-center rounded-full bg-[#F7EDD5] px-2.5 py-1 text-[11px] font-semibold text-[#8A6200]">
      {children}
    </span>
  );
}
